import { StoriesHash } from '@storybook/api';
import { Dispatch, MutableRefObject } from 'react';
export declare type ExpandedState = Record<string, boolean>;
export interface ExpandAction {
    ids: string[];
    value: boolean;
}
export interface ExpandedProps {
    containerRef: MutableRefObject<HTMLElement>;
    isBrowsing: boolean;
    refId: string;
    data: StoriesHash;
    rootIds: string[];
    highlightedItemId: string | null;
    setHighlightedItemId: (storyId: string) => void;
    selectedStoryId: string | null;
    onSelectStoryId: (storyId: string) => void;
}
export declare const useExpanded: ({ containerRef, isBrowsing, refId, data, rootIds, highlightedItemId, setHighlightedItemId, selectedStoryId, onSelectStoryId, }: ExpandedProps) => [
    Record<string, boolean>,
    Dispatch<ExpandAction>
];
